你是一位资深前端工程师。构建一个完全自包含的单 HTML 文件——无需构建工具，无需服务器——在浏览器中直接双击打开（file:// 协议）即可作为 Vue 3 后台管理系统演示运行。

要求：

- 仅使用 CDN 依赖：Vue 3、Vue Router 4（hash 模式）、Element Plus、Element Plus 图标库。**请使用稳定可靠的 CDN（如 unpkg 或 cdnjs），并确保脚本引入方式在 file:// 环境下不会产生跨域或模块加载报错（推荐使用全局变量方式或正确配置 importmap）。**
    
- 布局：顶部是导航条（包含系统 Logo 和用户信息），左侧是可折叠的菜单栏，右侧是主要的路由内容展示区。
    
- 左侧边栏菜单项（共 5 个）：案件管理 / 收支管理 / 请求管理 / 一般管理 / 用户管理。**（请为“案件管理”和“一般管理”各随意编造 2 个子菜单项，以体现多级菜单的折叠效果）。**
    
- 路由与交互：点击每个菜单项，右侧内容区域切换显示对应页面的内容骨架。
    
- 页面骨架内容：每个页面必须包含：页面标题（h2）以及一个表格或表单。**请填充适量的静态 Mock 数据（例如“案件管理”表格包含案件编号、状态、日期等列），无需真实逻辑，但要看起来像真实的业务页面。**
    
- 风格：企业管理系统视觉风格——白色侧边栏，#1a3a5c 标题和按钮强调色，排版简洁，无深色模式。
    
- 输出：一个完整的 HTML 文件，无外部依赖资源（除 CDN 外），**严格禁止使用"在此添加代码"或"..."之类的占位符省略代码**。
    

完成标准：文件在 Chrome 中双击打开后，顶部 Header 正常显示，菜单栏可折叠且能正常跳转，每个页面显示各自独立且饱满的内容骨架。