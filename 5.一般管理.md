> 你是一位资深前端工程师。现在我们需要实现系统中最重要的基础模块：**『一般管理（マスター管理）』**。为了贴合真实的日本业务场景，**请在生成的 UI 界面中（包括菜单名、表头、按钮、表单标签等）严格使用我提供的日文专有名词**。请修改单 HTML 代码，满足以下要求：
> 
> **1. 菜单结构调整：**
> 
> - 将左侧边栏的“一般管理”扩展为二级折叠菜单（メニュー展開），包含以下子项：
>     
>     - **取引先管理** (路由：`#/master/partner`)
>         
>     - **請求先管理** (路由：`#/master/billing-target`)
>         
>     - **請求項目管理** (路由：`#/master/items`)
>         
>     - **区分マスタ管理** (即数据字典，路由：`#/master/dictionary`)
>     
>     - 港・場所マスタ (`#/master/port`)
>     
>     - 船名マスタ (`#/master/vessel`)

>         
> 
> **2. 核心功能实现 - 『港・場所管理』 (Service Items)：**
> 
> - **页面布局：** 顶部搜索区（コード / 項目名），中部工具栏包含「新規」按钮，下方是数据表格。
>     
> - **表格字段：** No.、操作、項目コード（如 1001）、請求項目(日本語)（如 海上運賃）、デフォルト税区分（如 3:免税）、デフォルト単価。
>     
> - **交互逻辑：** 点击「新規」或「変更」，打开一个 Dialog 对话框。
>     
> - **重点：** 对话框中的“デフォルト税区分”必须是一个下拉选择框，其选项（Options）应联动自“区分マスタ管理”中定义的数据。
>     
> 
> **3. 核心功能实现 - 『取引先管理』 (Trading Partners)：**
> 
> - **页面布局：** 标准的 CRUD 界面。
> - 顶部搜索区（取引先コード、取引先名），中部工具栏包含「新規」按钮，下方是数据表格。
> - **表格字段：** No.、操作、取引先コード、取引先名、区分（下拉选项：顧客 / 仕入先 / 船社 / ドレー会社）、ステータス（有効 / 無効 的 Switch 开关）。
>     
> - **交互逻辑：** 同样使用 Dialog 进行数据的「新規」和「変更」。
>     
> 
> **4. 『区分マスタ管理』 (Dictionary) 的设计理念：**
> 
> - 这是一个左右两列布局：左侧是“区分タイプ”（如：税区分、通貨区分、案件ステータス），右侧是选中类型后对应的“No.、コード/名称（Code/Name 键值对）”明细列表。
> - 允许用户在这里通过按钮「行追加」添加新的下拉框选项。
>     
   **5.  核心功能实现 - 『港・場所管理』（port） ：**
> 
> - **页面布局：** 标准的 CRUD 界面。
> - 顶部搜索区（場所コード、場所名），中部工具栏包含「新規」按钮，下方是数据表格。
>     
> - **表格字段：** ：No.、操作、場所コード（如 JPYOK）、港・都市名(英)（如 YOKOHAMA）、国コード（如 JP）。
>     
> - **交互逻辑：** 同样使用 Dialog 进行数据的「新規」和「変更」。

**６.  核心功能实现 - 『船名管理』 （vessel） ：**
> 
> - **页面布局：** 标准的 CRUD 界面。
> - 顶部搜索区（コード、船名），中部工具栏包含「新規」按钮，下方是数据表格。
> - **表格字段：** ：No.、操作、コールサイン/IMO（如 9V7846）、船名（如 WAN HAI 365）、船社コード。
>     
> - **交互逻辑：** 同样使用 Dialog 进行数据的「新規」和「変更」。

> **7. 技术规范与 Mock 数据：**
> 
> - 风格保持一致：企业级简洁排版，#1a3a5c 标题和按钮强调色。
>     
> - 所有的 Master Data 需存储在 Vue 实例的全局响应式 Data 中，确保其他页面可以引用。
>     
> - 预置日文 Mock 数据（例如取引先预置“日高金属株式会社”，请求项目预置“1001 海上運賃”）。
>     
> - 严禁使用占位符，输出完整的、可直接双击运行的 HTML 代码。
>