你是一位资深前端工程师。现在我们需要实现系统的最后一个核心模块：**『用户管理（ユーザー管理）』**。请修改单 HTML 代码，满足以下要求（保持单文件、纯前端运行、无占位符）：

**1. 路由与菜单：**

- 对应左侧菜单的“ユーザー管理”（用户管理），路由为 `#/user-management`。
    

**2. 核心功能实现 - 『ユーザー管理』页面：**

- **页面布局：** 顶部搜索区（ユーザー名 / 権限类别），中部工具栏包含「新規（新增用户）」按钮，下方是数据表格。
    
- **表格字段：** 


     - No.
     - 操作（変更、PWリセット）
	 -  ユーザー名（用户名）
     -  **パスワード
    - 権限（权限：显示“管理者”或“業務担当者”）
        
    - ステータス（状态：有効 / 無効 的 Tag 标签）
        
    - 删除ボタン。
    - 
        

**3. 交互逻辑（Dialog 对话框）：**

- **「新規ユーザー」与「変更」对话框：** > - **ユーザー名：** 必填输入框。
    
    - **パスワード（密码）：** 新增时必填，编辑时可选。
        
    - **権限設定：** 使用 `el-radio-group` 或 `el-select` 选择，选项为：**システム管理者**、業務担当者**。
        
    - **ステータス：** 开关切换。
        
- **「パスワードリセット（重置密码）」：** 点击后弹出确认框，模拟重置成功的消息提示。
    

**4. 技术规范与样式：**

- **权限隔离预留：** 虽然目前不实现后端拦截，但在 Vue 的全局状态中，请为当前登录用户设置一个 `currentUser` 对象。
    
- **风格统一：** 继续保持企业级简洁设计，#1a3a5c 强调色。
    
- **Mock 数据：** 预置 2-3 个用户（例如：admin 设为系统管理者，staff01 设为营业担当者）。
    

**5. 最终交付：**

- 输出一个完整的、包含之前所有模块（案件、收支、请求、一般管理、用户管理）的 HTML 文件。确保左侧菜单点击后能正常切换所有页面，且每个页面都具备完整的功能骨架。
    
- **严禁占位符**，确保文件双击即用。